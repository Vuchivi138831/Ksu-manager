name: Build Manager Clean

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  # JOB 1: Biên dịch bộ công cụ ksud bằng Rust cho các kiến trúc Android
  build-ksud-binaries:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        target: [
          "aarch64-linux-android",      # 64-bit ARM (Hầu hết điện thoại)
          "armv7-linux-androideabi",    # 32-bit ARM (Các máy đời cũ)
          "x86_64-linux-android"        # Giả lập / Thiết bị Intel
        ]
    steps:
      - name: Checkout mã nguồn
        uses: actions/checkout@v4

      - name: Cài đặt Rust và Target
        run: |
          rustup update stable
          rustup target add ${{ matrix.target }}

      - name: Cài đặt công cụ Cross-compiler
        run: |
          cargo install cross --git https://github.com/cross-rs/cross --rev 66845c1

      - name: Biên dịch ksud bằng Rust
        run: |
          CROSS_NO_WARNINGS=0 cross build --target ${{ matrix.target }} --release --manifest-path ./userspace/ksud/Cargo.toml

      - name: Upload ksud Artifact
        uses: actions/upload-artifact@v4
        with:
          name: ksud-${{ matrix.target }}
          path: userspace/ksud/target/${{ matrix.target }}/release/ksud

  # JOB 2: Cài đặt NDK, gom ksud và build APK hoàn chỉnh (Tự tạo libkernelsu.so)
  build-apk:
    needs: build-ksud-binaries
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./manager

    steps:
      - name: Checkout mã nguồn
        uses: actions/checkout@v4

      - name: Setup Java 21
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      # Thiếp lập Android SDK và Android NDK (Cực kỳ quan trọng để dịch libkernelsu.so)
      - name: Setup Android NDK
        uses: android-actions/setup-android@v3
        with:
          log-accepted-android-sdk-licenses: false

      - name: Install NDK
        run: |
          sdkmanager --install "ndk;26.1.10909125" # Cài đặt phiên bản NDK chuẩn ổn định

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3

      # Tải các file ksud đã biên dịch ở Job 1 về
      - name: Download ksud aarch64
        uses: actions/download-artifact@v4
        with:
          name: ksud-aarch64-linux-android
          path: ksud-arm64

      - name: Download ksud armv7
        uses: actions/download-artifact@v4
        with:
          name: ksud-armv7-linux-androideabi
          path: ksud-armv7

      - name: Download ksud x86_64
        uses: actions/download-artifact@v4
        with:
          name: ksud-x86_64-linux-android
          path: ksud-x86_64

      # Tạo thư mục jniLibs và copy đổi tên ksud thành libksud.so
      - name: Copy ksud to app jniLibs
        run: |
          mkdir -p app/src/main/jniLibs/arm64-v8a
          mkdir -p app/src/main/jniLibs/armeabi-v7a
          mkdir -p app/src/main/jniLibs/x86_64
          
          cp -f ../ksud-arm64/ksud app/src/main/jniLibs/arm64-v8a/libksud.so
          cp -f ../ksud-armv7/ksud app/src/main/jniLibs/armeabi-v7a/libksud.so
          cp -f ../ksud-x86_64/ksud app/src/main/jniLibs/x86_64/libksud.so

      # Tiến hành build app bằng Gradle (Gradle lúc này có NDK sẽ tự động dịch ra libkernelsu.so)
      - name: Build Manager APK
        env:
          ANDROID_NDK_HOME: ${{ env.ANDROID_HOME }}/ndk/26.1.10909125
        run: |
          {
            echo 'org.gradle.parallel=true'
            echo 'org.gradle.vfs.watch=true'
            echo 'org.gradle.jvmargs=-Xmx2048m'
            echo "ndk.dir=$ANDROID_NDK_HOME"
          } >> gradle.properties
          ./gradlew clean assembleRelease

      # Xuất file APK ra ngoài cho bạn tải về
      - name: Upload Manager APK
        uses: actions/upload-artifact@v4
        with:
          name: KernelSU-Manager-Clean
          path: manager/app/build/outputs/apk/release/*.apk
